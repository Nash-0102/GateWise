<% layout('layouts/layout') %>
<% locals.title = 'Create Complaint' %>

<!-- Main Content Area -->
<main class="max-w-7xl mx-auto py-8 px-4 sm:px-6 lg:px-8 w-full">




    <div class="mb-6">
            <a href="/" class="inline-flex items-center gap-2 text-sm font-medium text-slate-400 hover:text-white transition-colors group">
                <svg class="w-4 h-4 group-hover:-translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
                </svg>
                Back to Dashboard
            </a>
        </div>

    <!-- Form Container Card -->
    <div class="max-w-2xl mx-auto glass-card rounded-2xl overflow-hidden shadow-card-dark">
        
        <!-- Form Header -->
        <div class="p-6 md:p-8 border-b border-white/10 bg-gradient-to-r from-dark-card/50 to-transparent">
            <div class="flex items-center space-x-3 mb-2">
                <div class="w-10 h-10 rounded-lg bg-gradient-to-br from-neon-teal to-teal-600 flex items-center justify-center shadow-lg shadow-neon-teal/30">
                    <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
                    </svg>
                </div>
                <h2 class="text-2xl font-bold text-gray-100">Create New Complaint</h2>
            </div>
            <p class="mt-2 text-gray-400">Please provide all details about your issue.</p>
        </div>

        <!-- Form Body -->
        <form action="/complaints" method="post" class="p-6 md:p-8 space-y-6">
            
            <!-- User -->
            <div>
                <label for="user" class="block text-sm font-semibold text-gray-300 mb-2 flex items-center space-x-2">
                    <svg class="w-4 h-4 text-neon-teal" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                    </svg>
                    <span>Your Name</span>
                </label>
                <input 
                    type="text" 
                    id="user"
                    name="complaints[user]" 
                    placeholder="Enter your name"
                    class="w-full px-4 py-3 bg-dark-surface/50 border border-white/10 rounded-xl text-gray-100 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-neon-teal/50 focus:border-neon-teal/50 transition-all duration-300 hover:border-neon-teal/30"
                    required
                >
            </div>

            <!-- Title -->
            <div>
                <label for="title" class="block text-sm font-semibold text-gray-300 mb-2 flex items-center space-x-2">
                    <svg class="w-4 h-4 text-neon-teal" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 8h10M7 12h4m1 8l-4-4H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-3l-4 4z"></path>
                    </svg>
                    <span>Complaint Title</span>
                </label>
                <input 
                    type="text" 
                    id="title"
                    name="complaints[title]" 
                    placeholder="e.g., Leaky pipe in B-Wing bathroom"
                    class="w-full px-4 py-3 bg-dark-surface/50 border border-white/10 rounded-xl text-gray-100 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-neon-teal/50 focus:border-neon-teal/50 transition-all duration-300 hover:border-neon-teal/30"
                    required
                >
            </div>

            <!-- Description -->
            <div>
                <label for="description" class="block text-sm font-semibold text-gray-300 mb-2 flex items-center space-x-2">
                    <svg class="w-4 h-4 text-neon-teal" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h7"></path>
                    </svg>
                    <span>Full Description</span>
                </label>
                <textarea 
                    id="description"
                    name="complaints[description]" 
                    rows="6"
                    placeholder="Enter a detailed description of the problem. Include location, severity, and any other relevant information..."
                    class="w-full px-4 py-3 bg-dark-surface/50 border border-white/10 rounded-xl text-gray-100 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-neon-teal/50 focus:border-neon-teal/50 transition-all duration-300 hover:border-neon-teal/30 resize-none"
                    required
                ></textarea>
            </div>
            
            <!-- Hidden Status Field -->
            <input type="hidden" name="complaints[status]" value="Pending">
            
            <!-- Submit Button -->
            <div class="pt-4">
                <button 
                    type="submit"
                    class="w-full neon-button bg-gradient-to-r from-neon-teal to-teal-600 hover:from-teal-600 hover:to-neon-teal text-white font-semibold py-3 px-6 rounded-xl shadow-lg shadow-neon-teal/30 hover:shadow-neon-teal/50 focus:outline-none focus:ring-2 focus:ring-neon-teal focus:ring-offset-2 focus:ring-offset-dark-bg transition-all duration-300 flex items-center justify-center space-x-2"
                >
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
                    </svg>
                    <span>Submit Complaint</span>
                </button>
            </div>
        </form>

        <!-- Back link -->
        <div class="px-6 py-4 border-t border-white/10 bg-dark-surface/50 text-center">
            <a href="/complaints" class="inline-flex items-center space-x-2 text-sm font-medium text-gray-400 hover:text-neon-teal transition-colors duration-300">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
                </svg>
                <span>Cancel and go back to complaints</span>
            </a>
        </div>
    </div>

</main>